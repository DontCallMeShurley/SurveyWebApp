@{
    ViewData["Title"] = "Рейтинг учителей";
}
@(Html.DevExtreme().DataGrid()
    .ID("gridContainer")
    .DataSource(d => d.Mvc()
        .Controller("ReportApi")
        .LoadAction("GetRaitingTeachers")
    )
    .FilterRow(x => x.Visible(true))
    .Export(x=>x.Enabled(true))
    .ShowBorders(true)
    )