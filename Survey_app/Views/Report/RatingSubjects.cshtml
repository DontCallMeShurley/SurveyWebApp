@{
    ViewData["Title"] = "Рейтинг предметов";
}

@(Html.DevExtreme().DataGrid()
    .ID("gridContainer")
    .DataSource(d => d.Mvc()
        .Controller("ReportApi")
        .LoadAction("GetRaitingSubjects")
    )
    .FilterRow(x => x.Visible(true))
    .OnInitNewRow("function(e) { e.data = 0; }")
    .Export(x=>x.Enabled(true))
   )

    